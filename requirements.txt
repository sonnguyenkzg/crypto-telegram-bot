# Core dependencies from original bot
requests>=2.31.0
python-dotenv>=1.0.0

# Telegram bot dependencies
python-telegram-bot>=20.7

# Existing functionality (will reuse from original bot)
# No additional dependencies needed for:
# - Decimal calculations (built-in)
# - JSON handling (built-in)
# - CSV operations (built-in)
# - Datetime operations (built-in)